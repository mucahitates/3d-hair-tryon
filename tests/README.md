# Tests

This folder contains automated tests for the system:

- Backend API tests
- AI pipeline integration tests
- Mobile app tests

These tests ensure that the platform works correctly as new features are added.
