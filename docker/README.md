# Docker Infrastructure

This folder contains all Docker and Docker Compose files required to run the full platform locally or in production.

It includes containers for:
- FastAPI backend
- GPU worker
- PostgreSQL
- Redis
- MinIO (S3 compatible storage)

The system is fully containerized for reproducible deployments.
